[build]
  base = "artifacts/butterflybot-site"
  command = "pnpm install && pnpm run build"
  publish = "dist/publish"

[build.environment]
  NODE_VERSION = "20"
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
